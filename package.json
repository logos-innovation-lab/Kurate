{
  "name": "@logos-labs/kurate-monorepo",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "test": "pnpm --stream -r test",
    "check": "pnpm --stream -r check",
    "lint": "pnpm --stream -r lint",
    "knip": "pnpm --stream -r knip"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "dotenv": "^16.0.3"
  },
  "engines": {
    "node": "16.x - 19.x"
  },
  "pnpm": {
    "patchedDependencies": {
      "@zk-kit/incremental-merkle-tree@0.4.3": "patches/@zk-kit__incremental-merkle-tree@0.4.3.patch",
      "@zk-kit/identity@1.4.1": "patches/@zk-kit__identity@1.4.1.patch",
      "@zk-kit/protocols@1.11.1": "patches/@zk-kit__protocols@1.11.1.patch"
    }
  }
}